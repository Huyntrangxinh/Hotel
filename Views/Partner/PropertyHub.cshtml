@{
    var p = ViewBag.Property as HotelBooking.Models.Property;
    ViewData["Title"] = p?.Name + " • Trung tâm quản lý";
    Layout = null; // Ẩn navbar/global layout cho màn PropertyHub
}

<!DOCTYPE html>
<html lang="vi">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewData["Title"]</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Bootstrap Icons -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css" rel="stylesheet">
    <!-- Google Fonts: Inter -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">

    <style>
        :root {
            --primary-blue: #003580;
            --light-blue: #006ce4;
            --body-bg: #f0f2f5;
            --card-border: #e0e0e0;
            --text-secondary: #6c757d;
        }

        body {
            background-color: var(--body-bg);
            font-family: 'Inter', sans-serif;
        }

        /* Header */
        .extranet-header {
            background-color: var(--primary-blue);
            color: white;
            padding: 0.75rem 1.5rem;
            position: sticky;
            top: 0;
            z-index: 1030;
        }

        .header-top {
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .header-brand {
            font-weight: 700;
            font-size: 1.25rem;
        }

        .header-actions {
            display: flex;
            align-items: center;
            gap: 1rem;
        }

        .header-actions .form-control {
            background-color: rgba(255, 255, 255, 0.1);
            border: 1px solid rgba(255, 255, 255, 0.3);
            color: white;
            width: 250px;
        }

        .header-actions .form-control::placeholder {
            color: rgba(255, 255, 255, 0.7);
        }

        .header-icon {
            font-size: 1.25rem;
            color: white;
            text-decoration: none;
        }

        /* BẮT ĐẦU CSS MỚI CHO ICON */
        .extranet-eye-icon {
            font-size: 1rem;
            /* Kích thước icon */
            color: rgba(255, 255, 255, 0.7);
            /* Màu trắng mờ */
            vertical-align: middle;
            /* Căn chỉnh theo chiều dọc */
        }

        /* KẾT THÚC CSS MỚI */

        /* Navigation */
        .extranet-nav {
            background-color: white;
            border-bottom: 1px solid var(--card-border);
            padding: 0 1.5rem;
            display: flex;
            align-items: center;
            flex-wrap: wrap;
        }

        .nav-property-info {
            padding: 0.75rem 1rem 0.75rem 0;
            border-right: 1px solid var(--card-border);
            margin-right: 1rem;
        }

        .nav-property-info h6 {
            margin-bottom: 0;
            font-weight: 600;
        }

        .nav-main {
            display: flex;
            gap: 0.5rem;
            flex-grow: 1;
            flex-wrap: wrap;
        }

        .nav-link-item {
            padding: 1rem 0.75rem;
            color: var(--text-secondary);
            text-decoration: none;
            font-weight: 500;
            font-size: 0.9rem;
            display: flex;
            align-items: center;
            gap: 0.5rem;
            border-bottom: 3px solid transparent;
        }

        .nav-link-item:hover {
            color: #212529;
        }

        .nav-link-item.active {
            border-bottom-color: var(--light-blue);
            color: #212529;
            font-weight: 600;
        }

        /* Main Content */
        .main-container {
            padding: 1.5rem;
        }

        .card {
            border: 1px solid var(--card-border);
            border-radius: 0.5rem;
        }

        .card-header {
            font-weight: 600;
        }

        /* Alert Cards */
        .alert-card {
            background-color: #fff8e1;
            border: 1px solid #ffecb3;
            color: #6d4c41;
        }

        .alert-card-danger {
            background-color: #fce4ec;
            border: 1px solid #f8bbd0;
            color: #880e4f;
        }

        /* Empty State */
        .empty-state {
            padding: 2rem;
            text-align: center;
            color: var(--text-secondary);
        }

        .empty-state i {
            font-size: 2.5rem;
        }

        .hover-bg-light:hover {
            background-color: #f8f9fa !important;
            cursor: pointer;
        }
    </style>
</head>

<body>

    <header class="extranet-header shadow-sm">
        <div class="header-top">
            <div class="d-flex align-items-center gap-3">
                <a href="@Url.Action("Index", "Home")" class="btn btn-outline-light btn-sm">
                    <i class="bi bi-arrow-left"></i> Quay lại
                </a>
                <div class="header-brand ms-1">Booking.com</div>

                <!-- ĐOẠN CODE ĐÃ ĐƯỢC THAY ĐỔI -->
                <div class="d-flex align-items-center text-white fw-semibold">
                    <span>@p?.Name</span>
                    <a href="@Url.Action("Hotel", "Public", new { id = p?.Id })" title="Xem trang hiển thị"
                        class="text-decoration-none">
                        <i class="bi bi-eye ms-2 extranet-eye-icon"></i>
                    </a>
                    <span class="badge bg-success ms-2">Mở / Có thể đặt phòng</span>
                </div>
                <!-- KẾT THÚC THAY ĐỔI -->

            </div>
            <div class="header-actions">
                <input class="form-control form-control-sm" placeholder="Tìm trang và đơn đặt phòng">
                <a href="#" class="header-icon"><i class="bi bi-search"></i></a>
                <a href="#" class="header-icon"><i class="bi bi-question-circle"></i></a>
                <a href="#" class="header-icon"><i class="bi bi-bell"></i></a>
                <div class="dropdown">
                    <a href="#" class="header-icon dropdown-toggle" id="userMenu" data-bs-toggle="dropdown"
                        aria-expanded="false">
                        <i class="bi bi-person-circle"></i>
                    </a>
                    <ul class="dropdown-menu dropdown-menu-end shadow" aria-labelledby="userMenu">
                        <li><a class="dropdown-item" href="#"><i class="bi bi-person me-2"></i> Tài khoản</a></li>
                        <li><a class="dropdown-item" href="#"><i class="bi bi-bag me-2"></i> Đặt chỗ & Chuyến đi</a>
                        </li>
                        <li><a class="dropdown-item" href="#"><i class="bi bi-stars me-2"></i> Chương trình khách hàng
                                thân thiết Genius</a></li>
                        <li><a class="dropdown-item" href="#"><i class="bi bi-wallet2 me-2"></i> Tặng thưởng & Ví</a>
                        </li>
                        <li><a class="dropdown-item" href="#"><i class="bi bi-people me-2"></i> Đánh giá</a></li>
                        <li><a class="dropdown-item" href="#"><i class="bi bi-heart me-2"></i> Đã lưu</a></li>
                        <li>
                            <hr class="dropdown-divider" />
                        </li>
                        <li><a class="dropdown-item" href="@Url.Action("MyProperties", "Partner")"><i
                                    class="bi bi-shop-window me-2"></i> Cơ sở lưu trú của bạn</a></li>
                        <li><a class="dropdown-item" href="@Url.Action("GoToHub", "Partner")"><i
                                    class="bi bi-kanban me-2"></i> Quản lý cơ sở lưu trú của bạn</a></li>
                        <li>
                            <hr class="dropdown-divider" />
                        </li>
                        <li>
                            <form asp-area="" asp-controller="Account" asp-action="Logout" method="post">
                                <button type="submit" class="dropdown-item text-danger">
                                    <i class="bi bi-box-arrow-right me-2"></i> Đăng xuất
                                </button>
                            </form>
                        </li>
                    </ul>
                </div>
            </div>
        </div>
    </header>

    <nav class="extranet-nav shadow-sm">
        <div class="nav-main">
            <a class="nav-link-item active" href="#"><i class="bi bi-house-door-fill"></i> Trang chủ</a>
            <a class="nav-link-item" href="@Url.Action("Calendar", "Partner", new { propertyId = p?.Id })"><i
                    class="bi bi-calendar3"></i> Giá & Tình trạng phòng trống</a>
            <a class="nav-link-item" href="#"><i class="bi bi-award"></i> Chương trình khuyến mãi</a>
            <a class="nav-link-item" href="#"><i class="bi bi-journal-text"></i> Đặt phòng</a>
            <a class="nav-link-item"
                href="@Url.Action("PropertyData", "Partner", new { propertyId = p?.Id, tab = "rooms" })"><i
                    class="bi bi-building"></i> Chỗ nghỉ</a>
            <a class="nav-link-item"
                href="@Url.Action("PropertyData", "Partner", new { propertyId = p?.Id, tab = "pricing" })"><i
                    class="bi bi-sliders"></i> Cấu hình giá</a>
            <a class="nav-link-item" href="#"><i class="bi bi-graph-up-arrow"></i> Thúc đẩy hiệu suất</a>
        </div>
    </nav>

    @* Ẩn thông báo success trên Hub theo yêu cầu *@

    <div class="container-fluid main-container">
        <div class="row g-4">
            <!-- Left Column -->
            <div class="col-lg-8">
                <!-- Bookings -->
                <div class="card mb-4">
                    <div class="card-header d-flex justify-content-between align-items-center">
                        <span>Đặt phòng</span>
                        <input type="date" class="form-control form-control-sm" style="width: 150px;" value="@DateTime.Today.ToString("yyyy-MM-dd")">
                    </div>
                    <div class="card-body">
                        @{
                            var recentBookings = ViewBag.RecentBookings as List<dynamic>;
                        }
                        @if (recentBookings != null && recentBookings.Any())
                        {
                            <div class="booking-list">
                                @{
                                    var uniqueBookings = recentBookings
                                        .GroupBy(b => b.Booking.Id)
                                        .Select(g => g.First())
                                        .Take(5)
                                        .ToList();
                                }
                                @foreach (var item in uniqueBookings)
                                {
                                    @if (!string.IsNullOrEmpty(item.Booking.BookingCode))
                                    {
                                        <a href="@Url.Action("BookingDetails", "Partner", new { bookingCode = item.Booking.BookingCode })" class="text-decoration-none">
                                            <div class="booking-item d-flex justify-content-between align-items-center p-2 border-bottom hover-bg-light">
                                                <div>
                                                    <h6 class="mb-1 text-dark">@item.RoomName</h6>
                                                    <small class="text-muted">@item.Booking.BookingCode • @item.Booking.CheckIn.ToString("dd/MM/yyyy") - @item.Booking.CheckOut.ToString("dd/MM/yyyy")</small>
                                                </div>
                                                <div class="text-end">
                                                    <span class="badge bg-primary">@item.Booking.TotalPrice.ToString("N0") VND</span>
                                                </div>
                                            </div>
                                        </a>
                                    }
                                }
                            </div>
                        }
                        else
                        {
                            <div class="empty-state">
                                <i class="bi bi-moon-stars"></i>
                                <p class="mt-2">Quý vị không có khách đến trong khoảng thời gian được chọn</p>
                            </div>
                        }
                        <a class="btn btn-outline-primary btn-sm mt-2" href="@Url.Action("AllBookings", "Partner", new { propertyId = ViewBag.Property.Id })">Xem tất cả đặt phòng</a>
                    </div>
                </div>

                <!-- Performance -->
                <div class="card">
                    <div class="card-header">Hiệu suất hoạt động (Trong 30 ngày qua)</div>
                    <div class="card-body">
                        <div class="row text-center">
                            <div class="col">
                                <div class="fw-bold fs-5">@ViewBag.AverageDailyRate.ToString("N0")</div>
                                <small class="text-muted">Giá trung bình hàng ngày</small>
                            </div>
                            <div class="col">
                                <div class="fw-bold fs-5">@ViewBag.CancellationRate.ToString("F1")%</div>
                                <small class="text-muted">Tỷ lệ hủy</small>
                            </div>
                            <div class="col">
                                <div class="fw-bold fs-5">@ViewBag.TotalRevenue.ToString("N0")</div>
                                <small class="text-muted">Doanh thu (sau hoa hồng)</small>
                            </div>
                            <div class="col">
                                <div class="fw-bold fs-5">@ViewBag.TotalNights</div>
                                <small class="text-muted">Số đêm đã lưu trú</small>
                            </div>
                        </div>
                        <hr>
                        
                        <!-- Biểu đồ -->
                        <div class="mt-4">
                            <h6 class="mb-3">Biểu đồ doanh thu 7 ngày qua (sau hoa hồng)</h6>
                            <canvas id="revenueChart" width="400" height="200"></canvas>
                        </div>
                        
                        <div class="d-flex justify-content-between align-items-center mt-3">
                            <div>
                                <h6 class="fw-bold">Để xuất hỗ trợ Quý vị tăng hiệu suất</h6>
                                <p class="mb-0 small">Thu hút 71% khách đang tìm kiếm dịch vụ giá trị gia tăng.</p>
                            </div>
                            <a href="#" class="btn btn-outline-primary btn-sm">Bắt đầu với dịch vụ giá trị gia tăng</a>
                        </div>
                    </div>
                </div>

            </div>
            <!-- Right Column -->
            <div class="col-lg-4">
                <div class="card mb-4">
                    <div class="card-header">Đang chờ xử lý</div>
                    <div class="card-body">
                        <p class="small text-muted">Hoàn tất! Thông báo sẽ hiển thị tại đây khi có nhiệm vụ mới.</p>
                    </div>
                </div>
                <div class="card text-center">
                    <div class="card-header">Đề xuất</div>
                    <div class="card-body">
                        <img src="https://placehold.co/150x100/E8F0FE/003580?text=Cơ+hội" class="mb-3"
                            alt="Opportunities">
                        <h6 class="fw-bold">Khám phá Trung tâm Cơ hội</h6>
                        <p class="small text-muted">Để tìm vô số đề xuất về cách cải thiện tất cả các chỉ số chính của
                            Quý vị.</p>
                        <a href="#" class="btn btn-outline-primary w-100">Mở Trung tâm Cơ hội</a>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Bootstrap JS (enable dropdowns) -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <!-- Chart.js -->
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <script>
        // Ensure dropdown works even if dynamically loaded
        document.querySelectorAll('[data-bs-toggle="dropdown"]').forEach(function (el) {
            new bootstrap.Dropdown(el);
        });

        // Tạo biểu đồ doanh thu
        const chartData = @Html.Raw(Json.Serialize(ViewBag.ChartData));
        const ctx = document.getElementById('revenueChart').getContext('2d');
        
        new Chart(ctx, {
            type: 'line',
            data: {
                labels: chartData.map(item => item.date),
                datasets: [{
                    label: 'Doanh thu (VND) - Sau hoa hồng',
                    data: chartData.map(item => item.revenue),
                    borderColor: 'rgb(75, 192, 192)',
                    backgroundColor: 'rgba(75, 192, 192, 0.2)',
                    tension: 0.1,
                    fill: true
                }, {
                    label: 'Số đặt phòng',
                    data: chartData.map(item => item.bookings),
                    borderColor: 'rgb(255, 99, 132)',
                    backgroundColor: 'rgba(255, 99, 132, 0.2)',
                    tension: 0.1,
                    fill: false,
                    yAxisID: 'y1'
                }]
            },
            options: {
                responsive: true,
                plugins: {
                    title: {
                        display: true,
                        text: 'Doanh thu (sau hoa hồng) và số đặt phòng 7 ngày qua'
                    },
                    legend: {
                        display: true
                    }
                },
                scales: {
                    y: {
                        type: 'linear',
                        display: true,
                        position: 'left',
                        title: {
                            display: true,
                            text: 'Doanh thu (VND) - Sau hoa hồng'
                        },
                        ticks: {
                            callback: function(value) {
                                return value.toLocaleString('vi-VN') + ' VND';
                            }
                        }
                    },
                    y1: {
                        type: 'linear',
                        display: true,
                        position: 'right',
                        title: {
                            display: true,
                            text: 'Số đặt phòng'
                        },
                        grid: {
                            drawOnChartArea: false,
                        },
                    }
                }
            }
        });
    </script>
</body>

</html>
