@{
    ViewData["Title"] = "Đăng ký thành công";
}

<section class="py-5" style="background-color: #f8f9fa;">
    <div class="container d-flex justify-content-center align-items-center" style="min-height: 80vh;">

        <div class="bg-white p-4 p-md-5 rounded-4 shadow-sm border text-center" style="max-width: 550px; width: 100%;">

            <div class="mb-4">
                <i class="bi bi-calendar2-check-fill" style="font-size: 80px; color: #0d6efd;"></i>
            </div>

            <h3 class="fw-bold mb-3">Đã gửi đăng ký!</h3>

            <p class="text-muted">
                Hồ sơ đăng ký của bạn đã được gửi đi và đang được chúng tôi xem xét.
                Chúng tôi sẽ liên hệ với bạn qua email trong thời gian sớm nhất.
            </p>
            <p class="text-muted small">
                Trong thời gian chờ đợi, bạn có thể hoàn tất thông tin
                cần thiết khác cho cơ sở lưu trú của mình.
            </p>

            <div class="d-grid gap-2 mt-4">
                @if (ViewBag.LatestPropertyId != null)
                {
                    <a asp-action="PropertyDetail" asp-route-id="@ViewBag.LatestPropertyId" class="btn btn-primary btn-lg">Xem thông tin cơ sở lưu trú</a>
                }
                else
                {
                    <a asp-action="MyProperties" class="btn btn-primary btn-lg">Xem thông tin cơ sở lưu trú</a>
                }
                <a asp-controller="Home" asp-action="Index" class="btn btn-outline-secondary">Quay trở lại trang chủ</a>
            </div>
        </div>
    </div>
</section>